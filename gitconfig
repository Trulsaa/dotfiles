[credential]
  helper = osxkeychain
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[github]
  user = Trulsaa
[alias]
  co = checkout
  cm = commit -am
  lg = log --oneline --reverse -20
